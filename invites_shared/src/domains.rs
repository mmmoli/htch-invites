use crate::Subdomain;

pub struct Domain {
    pub name: String,
}

impl Domain {
    pub fn create_subdomain(_id: &str) -> Subdomain {
        todo!()
    }

    pub fn list_subdomains() -> Vec<Subdomain> {
        todo!()
    }
}
